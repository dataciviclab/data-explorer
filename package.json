{
  "name": "data-explorer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "evidence build",
    "build:strict": "evidence build:strict",
    "dev": "evidence dev --open /",
    "presources": "node scripts/sync_gcs_cache.mjs",
    "test": "npm run validate:catalog && evidence build",
    "sources": "evidence sources",
    "sources:strict": "evidence sources --strict",
    "preview": "evidence preview",
    "validate:catalog": "node scripts/validate_catalog.mjs"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": "20.x"
  },
  "type": "module",
  "dependencies": {
    "@evidence-dev/core-components": "^5.4.2",
    "@evidence-dev/duckdb": "^2.0.1",
    "@evidence-dev/evidence": "^40.1.8",
    "git-remote-origin-url": "^4.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "3.1.2",
    "autoprefixer": "10.5.0",
    "postcss": "8.5.12",
    "tailwindcss": "3.4.18"
  }
}
